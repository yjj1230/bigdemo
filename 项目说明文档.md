# Shopdemo 电商系统 - 项目说明文档

## 📋 项目概述

Shopdemo 是一个基于 Spring Boot 开发的电商系统，提供完整的商品管理、订单处理、用户管理等功能。系统采用前后端分离架构，提供 RESTful API 接口，支持用户注册登录、商品浏览、购物车、订单管理、商品评价、收藏等核心电商功能。

### 技术栈

- **后端框架**: Spring Boot 3.x
- **数据库**: MySQL
- **缓存**: Redis
- **ORM框架**: MyBatis
- **API文档**: Swagger/OpenAPI 3.0
- **认证授权**: JWT (JSON Web Token)
- **限流**: Redis + Lua脚本实现令牌桶算法

---

## 🎯 功能清单

### 1. 用户管理模块

| 功能 | 说明 |
|------|------|
| 用户注册 | 新用户账号注册 |
| 用户登录 | 用户名密码登录，返回JWT Token |
| 获取用户信息 | 获取当前登录用户的详细信息 |
| 更新用户信息 | 修改用户个人信息 |

### 2. 商品管理模块

| 功能 | 说明 |
|------|------|
| 获取所有商品 | 获取系统中所有商品列表 |
| 根据ID获取商品 | 根据商品ID获取商品详细信息 |
| 根据分类获取商品 | 根据分类ID获取该分类下的所有商品 |
| 搜索商品 | 根据关键词搜索商品 |
| 分页获取商品 | 支持分页查询商品列表 |
| 添加商品 | 添加新商品到系统 |
| 更新商品 | 更新商品信息 |
| 删除商品 | 软删除商品（标记为已删除状态） |
| 上架商品 | 将商品状态改为上架 |
| 下架商品 | 将商品状态改为下架 |
| 更新库存 | 更新商品库存数量 |

### 3. 分类管理模块

| 功能 | 说明 |
|------|------|
| 获取所有分类 | 获取系统中所有商品分类 |
| 根据ID获取分类 | 根据分类ID获取分类详细信息 |
| 获取子分类 | 根据父分类ID获取所有子分类 |
| 添加分类 | 添加新的商品分类 |
| 更新分类 | 更新商品分类信息 |
| 删除分类 | 根据ID删除商品分类 |

### 4. 购物车模块

| 功能 | 说明 |
|------|------|
| 获取购物车 | 获取当前登录用户的购物车商品列表 |
| 添加商品到购物车 | 将商品添加到当前用户的购物车 |
| 更新购物车商品数量 | 更新购物车中商品的数量 |
| 移除购物车商品 | 从购物车中移除指定商品 |
| 清空购物车 | 清空当前用户购物车中的所有商品 |

### 5. 订单管理模块

| 功能 | 说明 |
|------|------|
| 创建订单 | 创建新订单 |
| 从购物车创建订单 | 将购物车中的商品创建为订单 |
| 根据ID获取订单 | 根据订单ID获取订单详细信息 |
| 根据订单号获取订单 | 根据订单号查询订单 |
| 获取用户订单列表 | 获取当前登录用户的所有订单 |
| 获取所有订单 | 获取系统中所有订单（管理员功能） |
| 获取订单详情 | 获取订单的商品明细 |
| 支付订单 | 支付指定订单 |
| 订单发货 | 管理员对订单进行发货 |
| 完成订单 | 确认收货，完成订单 |
| 取消订单 | 取消指定订单 |
| 获取订单支付剩余时间 | 获取订单支付剩余时间（秒） |
| 订单超时自动取消 | 订单创建15分钟后未支付自动取消（定时任务） |

### 6. 地址管理模块

| 功能 | 说明 |
|------|------|
| 获取用户地址列表 | 获取当前登录用户的所有地址 |
| 根据ID获取地址 | 根据地址ID获取地址详细信息 |
| 添加地址 | 为当前用户添加新地址 |
| 更新地址 | 更新地址信息 |
| 删除地址 | 根据ID删除地址 |
| 设置默认地址 | 将指定地址设置为默认地址 |

### 7. 商品评价模块

| 功能 | 说明 |
|------|------|
| 添加评价 | 用户对商品进行评价 |
| 获取商品评价列表 | 分页获取指定商品的评价列表 |
| 获取用户评价列表 | 分页获取当前用户的评价列表 |
| 更新评价 | 用户更新自己的评价 |
| 删除评价 | 用户删除自己的评价 |
| 点赞评价 | 用户对评价进行点赞 |
| 商家回复评价 | 商家回复用户评价 |
| 获取商品平均评分 | 获取指定商品的平均评分 |

### 8. 商品收藏模块

| 功能 | 说明 |
|------|------|
| 添加收藏 | 用户收藏指定商品 |
| 取消收藏 | 用户取消收藏指定商品 |
| 检查是否已收藏 | 检查用户是否已收藏指定商品 |
| 获取用户收藏列表 | 分页获取当前用户的收藏商品列表 |
| 删除收藏 | 根据收藏ID删除收藏 |

### 9. 搜索历史模块

| 功能 | 说明 |
|------|------|
| 添加搜索历史 | 记录用户搜索关键词 |
| 获取用户搜索历史 | 获取当前用户的搜索历史 |
| 删除搜索历史 | 删除指定的搜索历史记录 |
| 清空搜索历史 | 清空当前用户的所有搜索历史 |
| 获取热门搜索关键词 | 获取热门搜索关键词 |

### 10. AI智能客服模块

| 功能 | 说明 |
|------|------|
| AI对话 | 与AI智能客服进行对话，获取商品推荐、购物指导等服务 |
| AI响应缓存 | 缓存AI响应，减少重复请求，提升响应速度 |

---

## 🔌 API接口说明

### 认证说明

所有需要身份验证的接口都需要在请求头中携带JWT Token：

```
Authorization: Bearer <token>
```

### 通用响应格式

所有接口返回统一的JSON格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

---

### 1. 用户管理 API

#### 1.1 用户登录

**接口地址**: `POST /user/login`

**请求参数**:
```json
{
  "username": "string",
  "password": "string"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "登录成功",
  "data": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```

**限流**: 60秒内最多5次请求（基于IP）

---

#### 1.2 用户注册

**接口地址**: `POST /user/register`

**请求参数**:
```json
{
  "username": "string",
  "password": "string",
  "email": "string",
  "phone": "string"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "注册成功",
  "data": null
}
```

---

#### 1.3 获取用户信息

**接口地址**: `GET /user/info`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "username": "user",
    "email": "user@example.com",
    "phone": "13800138000",
    "role": 0,
    "status": 1
  }
}
```

---

#### 1.4 更新用户信息

**接口地址**: `PUT /user/update`

**请求头**: `Authorization: Bearer <token>`

**请求参数**:
```json
{
  "email": "string",
  "phone": "string"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

### 2. 商品管理 API

#### 2.1 获取所有商品

**接口地址**: `GET /product/list`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "商品名称",
      "price": 99.99,
      "stock": 100,
      "status": 1,
      "categoryId": 1
    }
  ]
}
```

---

#### 2.2 根据ID获取商品

**接口地址**: `GET /product/{id}`

**路径参数**: 
- `id`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "商品名称",
    "price": 99.99,
    "stock": 100,
    "status": 1,
    "categoryId": 1,
    "description": "商品描述",
    "mainImage": "图片URL"
  }
}
```

---

#### 2.3 根据分类获取商品

**接口地址**: `GET /product/category/{categoryId}`

**路径参数**: 
- `categoryId`: 分类ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "商品名称",
      "price": 99.99,
      "stock": 100,
      "status": 1,
      "categoryId": 1
    }
  ]
}
```

---

#### 2.4 搜索商品

**接口地址**: `GET /product/search`

**查询参数**: 
- `keyword`: 搜索关键词

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "商品名称",
      "price": 99.99,
      "stock": 100,
      "status": 1
    }
  ]
}
```

**限流**: 60秒内最多30次请求（基于IP）

---

#### 2.5 分页获取所有商品

**接口地址**: `GET /product/list/page`

**查询参数**: 
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [...],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10
  }
}
```

---

#### 2.6 添加商品

**接口地址**: `POST /product/add`

**请求参数**:
```json
{
  "name": "商品名称",
  "price": 99.99,
  "stock": 100,
  "categoryId": 1,
  "description": "商品描述",
  "mainImage": "图片URL"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

---

#### 2.7 更新商品

**接口地址**: `PUT /product/update`

**请求参数**:
```json
{
  "id": 1,
  "name": "商品名称",
  "price": 99.99,
  "stock": 100,
  "categoryId": 1,
  "description": "商品描述",
  "mainImage": "图片URL"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

#### 2.8 删除商品

**接口地址**: `DELETE /product/{id}`

**路径参数**: 
- `id`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 2.9 上架商品

**接口地址**: `PUT /product/onShelf/{id}`

**路径参数**: 
- `id`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "上架成功",
  "data": null
}
```

---

#### 2.10 下架商品

**接口地址**: `PUT /product/offShelf/{id}`

**路径参数**: 
- `id`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "下架成功",
  "data": null
}
```

---

#### 2.11 更新商品库存

**接口地址**: `PUT /product/updateStock`

**查询参数**: 
- `id`: 商品ID
- `stock`: 库存数量

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

### 3. 分类管理 API

#### 3.1 获取所有分类

**接口地址**: `GET /category/list`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "name": "分类名称",
      "parentId": 0,
      "sort": 1
    }
  ]
}
```

---

#### 3.2 根据ID获取分类

**接口地址**: `GET /category/{id}`

**路径参数**: 
- `id`: 分类ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "name": "分类名称",
    "parentId": 0,
    "sort": 1
  }
}
```

---

#### 3.3 获取子分类

**接口地址**: `GET /category/parent/{parentId}`

**路径参数**: 
- `parentId`: 父分类ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 2,
      "name": "子分类名称",
      "parentId": 1,
      "sort": 1
    }
  ]
}
```

---

#### 3.4 添加分类

**接口地址**: `POST /category/add`

**请求参数**:
```json
{
  "name": "分类名称",
  "parentId": 0,
  "sort": 1
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

---

#### 3.5 更新分类

**接口地址**: `PUT /category/update`

**请求参数**:
```json
{
  "id": 1,
  "name": "分类名称",
  "parentId": 0,
  "sort": 1
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

#### 3.6 删除分类

**接口地址**: `DELETE /category/{id}`

**路径参数**: 
- `id`: 分类ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 4. 购物车管理 API

#### 4.1 获取购物车

**接口地址**: `GET /cart/list`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "productId": 1,
      "quantity": 2,
      "product": {
        "id": 1,
        "name": "商品名称",
        "price": 99.99
      }
    }
  ]
}
```

---

#### 4.2 添加商品到购物车

**接口地址**: `POST /cart/add`

**请求头**: `Authorization: Bearer <token>`

**请求参数**:
```json
{
  "productId": 1,
  "quantity": 2
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

---

#### 4.3 更新购物车商品数量

**接口地址**: `PUT /cart/update/{cartId}`

**路径参数**: 
- `cartId`: 购物车ID

**查询参数**: 
- `quantity`: 数量

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

#### 4.4 移除购物车商品

**接口地址**: `DELETE /cart/{cartId}`

**路径参数**: 
- `cartId`: 购物车ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 4.5 清空购物车

**接口地址**: `DELETE /cart/clear`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "清空成功",
  "data": null
}
```

---

### 5. 订单管理 API

#### 5.1 创建订单

**接口地址**: `POST /order/create`

**请求头**: `Authorization: Bearer <token>`

**请求参数**:
```json
{
  "addressId": 1,
  "remark": "订单备注",
  "items": [
    {
      "productId": 1,
      "quantity": 2
    }
  ]
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "创建订单成功",
  "data": {
    "id": 1,
    "orderNo": "202402221234567890",
    "totalAmount": 199.98,
    "status": 0
  }
}
```

**限流**: 60秒内最多10次请求（基于用户）

---

#### 5.2 从购物车创建订单

**接口地址**: `POST /order/createFromCart`

**请求头**: `Authorization: Bearer <token>`

**查询参数**: 
- `addressId`: 地址ID
- `remark`: 订单备注（可选）

**响应数据**:
```json
{
  "code": 200,
  "message": "创建订单成功",
  "data": {
    "id": 1,
    "orderNo": "202402221234567890",
    "totalAmount": 199.98,
    "status": 0
  }
}
```

---

#### 5.3 根据ID获取订单

**接口地址**: `GET /order/{id}`

**路径参数**: 
- `id`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "orderNo": "202402221234567890",
    "totalAmount": 199.98,
    "status": 0,
    "createTime": "2024-02-22T12:00:00"
  }
}
```

---

#### 5.4 根据订单号获取订单

**接口地址**: `GET /order/orderNo/{orderNo}`

**路径参数**: 
- `orderNo`: 订单号

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "orderNo": "202402221234567890",
    "totalAmount": 199.98,
    "status": 0
  }
}
```

---

#### 5.5 获取用户订单列表

**接口地址**: `GET /order/list`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "orderNo": "202402221234567890",
      "totalAmount": 199.98,
      "status": 0
    }
  ]
}
```

---

#### 5.6 获取所有订单（管理员）

**接口地址**: `GET /order/all`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "orderNo": "202402221234567890",
      "totalAmount": 199.98,
      "status": 0
    }
  ]
}
```

---

#### 5.7 获取订单详情

**接口地址**: `GET /order/items/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "orderId": 1,
      "productId": 1,
      "productName": "商品名称",
      "price": 99.99,
      "quantity": 2,
      "totalPrice": 199.98
    }
  ]
}
```

---

#### 5.8 支付订单

**接口地址**: `POST /order/pay/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "支付成功",
  "data": null
}
```

---

#### 5.9 订单发货（管理员）

**接口地址**: `POST /order/ship/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "发货成功",
  "data": null
}
```

---

#### 5.10 完成订单

**接口地址**: `POST /order/finish/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "订单已完成",
  "data": null
}
```

---

#### 5.11 取消订单

**接口地址**: `POST /order/cancel/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "订单已取消",
  "data": null
}
```

---

#### 5.12 获取订单支付剩余时间

**接口地址**: `GET /order/payTime/{orderId}`

**路径参数**: 
- `orderId`: 订单ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": 900
}
```

**说明**: 返回剩余秒数，-1表示已超时，-2表示订单状态异常

---

### 6. 地址管理 API

#### 6.1 获取用户地址列表

**接口地址**: `GET /address/list`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "receiverName": "张三",
      "receiverPhone": "13800138000",
      "province": "广东省",
      "city": "深圳市",
      "district": "南山区",
      "detailAddress": "科技园",
      "isDefault": 1
    }
  ]
}
```

---

#### 6.2 根据ID获取地址

**接口地址**: `GET /address/{id}`

**路径参数**: 
- `id`: 地址ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "id": 1,
    "userId": 1,
    "receiverName": "张三",
    "receiverPhone": "13800138000",
    "province": "广东省",
    "city": "深圳市",
    "district": "南山区",
    "detailAddress": "科技园",
    "isDefault": 1
  }
}
```

---

#### 6.3 添加地址

**接口地址**: `POST /address/add`

**请求头**: `Authorization: Bearer <token>`

**请求参数**:
```json
{
  "receiverName": "张三",
  "receiverPhone": "13800138000",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "detailAddress": "科技园",
  "isDefault": 0
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "添加成功",
  "data": null
}
```

---

#### 6.4 更新地址

**接口地址**: `PUT /address/update`

**请求参数**:
```json
{
  "id": 1,
  "receiverName": "张三",
  "receiverPhone": "13800138000",
  "province": "广东省",
  "city": "深圳市",
  "district": "南山区",
  "detailAddress": "科技园",
  "isDefault": 1
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

#### 6.5 删除地址

**接口地址**: `DELETE /address/{id}`

**路径参数**: 
- `id`: 地址ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 6.6 设置默认地址

**接口地址**: `PUT /address/default/{addressId}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `addressId`: 地址ID

**响应数据**:
```json
{
  "code": 200,
  "message": "设置成功",
  "data": null
}
```

---

### 7. 商品评价 API

#### 7.1 添加评价

**接口地址**: `POST /api/reviews`

**请求头**: `Authorization: Bearer <token>`

**请求参数**:
```json
{
  "productId": 1,
  "rating": 5,
  "content": "商品很好",
  "images": ["图片URL1", "图片URL2"],
  "isAnonymous": 0
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": 1
}
```

---

#### 7.2 获取商品评价列表

**接口地址**: `GET /api/reviews/product/{productId}`

**路径参数**: 
- `productId`: 商品ID

**查询参数**: 
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "productId": 1,
        "userId": 1,
        "username": "用户名",
        "rating": 5,
        "content": "商品很好",
        "images": ["图片URL"],
        "likeCount": 10,
        "createTime": "2024-02-22T12:00:00"
      }
    ],
    "total": 100,
    "pageNum": 1,
    "pageSize": 10
  }
}
```

---

#### 7.3 获取用户评价列表

**接口地址**: `GET /api/reviews/user`

**请求头**: `Authorization: Bearer <token>`

**查询参数**: 
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [...],
    "total": 50,
    "pageNum": 1,
    "pageSize": 10
  }
}
```

---

#### 7.4 更新评价

**接口地址**: `PUT /api/reviews/{id}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `id`: 评价ID

**请求参数**:
```json
{
  "content": "更新后的评价内容",
  "images": ["图片URL"],
  "isAnonymous": 0
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "更新成功",
  "data": null
}
```

---

#### 7.5 删除评价

**接口地址**: `DELETE /api/reviews/{id}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `id`: 评价ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 7.6 点赞评价

**接口地址**: `POST /api/reviews/{id}/like`

**路径参数**: 
- `id`: 评价ID

**响应数据**:
```json
{
  "code": 200,
  "message": "点赞成功",
  "data": null
}
```

---

#### 7.7 商家回复评价

**接口地址**: `POST /api/reviews/{id}/reply`

**路径参数**: 
- `id`: 评价ID

**查询参数**: 
- `reply`: 回复内容

**响应数据**:
```json
{
  "code": 200,
  "message": "回复成功",
  "data": null
}
```

---

#### 7.8 获取商品平均评分

**接口地址**: `GET /api/reviews/rating/{productId}`

**路径参数**: 
- `productId`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": 4.5
}
```

---

### 8. 商品收藏 API

#### 8.1 添加收藏

**接口地址**: `POST /api/favorites/{productId}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `productId`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "收藏成功",
  "data": null
}
```

---

#### 8.2 取消收藏

**接口地址**: `DELETE /api/favorites/{productId}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `productId`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "取消收藏成功",
  "data": null
}
```

---

#### 8.3 检查是否已收藏

**接口地址**: `GET /api/favorites/check/{productId}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `productId`: 商品ID

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": true
}
```

---

#### 8.4 获取用户收藏列表

**接口地址**: `GET /api/favorites`

**请求头**: `Authorization: Bearer <token>`

**查询参数**: 
- `pageNum`: 页码（默认1）
- `pageSize`: 每页数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "name": "商品名称",
        "price": 99.99,
        "mainImage": "图片URL"
      }
    ],
    "total": 20,
    "pageNum": 1,
    "pageSize": 10
  }
}
```

---

#### 8.5 删除收藏

**接口地址**: `DELETE /api/favorites/id/{id}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `id`: 收藏ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

### 9. 搜索历史 API

#### 9.1 添加搜索历史

**接口地址**: `POST /api/search-history`

**请求头**: `Authorization: Bearer <token>`

**查询参数**: 
- `keyword`: 搜索关键词

**响应数据**:
```json
{
  "code": 200,
  "message": "记录成功",
  "data": null
}
```

---

#### 9.2 获取用户搜索历史

**接口地址**: `GET /api/search-history`

**请求头**: `Authorization: Bearer <token>`

**查询参数**: 
- `limit`: 限制数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "userId": 1,
      "keyword": "手机",
      "searchCount": 5,
      "lastSearchTime": "2024-02-22T12:00:00"
    }
  ]
}
```

---

#### 9.3 删除搜索历史

**接口地址**: `DELETE /api/search-history/{id}`

**请求头**: `Authorization: Bearer <token>`

**路径参数**: 
- `id`: 搜索历史ID

**响应数据**:
```json
{
  "code": 200,
  "message": "删除成功",
  "data": null
}
```

---

#### 9.4 清空搜索历史

**接口地址**: `DELETE /api/search-history/clear`

**请求头**: `Authorization: Bearer <token>`

**响应数据**:
```json
{
  "code": 200,
  "message": "清空成功",
  "data": null
}
```

---

#### 9.5 获取热门搜索关键词

**接口地址**: `GET /api/search-history/hot`

**查询参数**: 
- `limit`: 限制数量（默认10）

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": [
    {
      "id": 1,
      "keyword": "手机",
      "searchCount": 1000
    }
  ]
}
```

---

### 10. AI智能客服 API

#### 10.1 AI对话

**接口地址**: `POST /agent/chat`

**请求参数**:
```json
{
  "message": "用户消息",
  "sessionId": "会话ID"
}
```

**响应数据**:
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "response": "AI回复内容",
    "sessionId": "会话ID"
  }
}
```

**特性**:
- AI响应缓存：相同问题会返回缓存结果，提升响应速度
- 支持商品推荐、购物指导等功能

---

## 📊 数据字典

### 订单状态 (OrderStatus)

| 状态码 | 枚举常量 | 描述 |
|--------|----------|------|
| 0 | PENDING_PAYMENT | 待支付 |
| 1 | PAID | 已支付 |
| 2 | SHIPPED | 已发货 |
| 3 | COMPLETED | 已完成 |
| 4 | CANCELLED | 已取消 |

### 用户状态 (UserStatus)

| 状态码 | 枚举常量 | 描述 |
|--------|----------|------|
| 0 | INACTIVE | 未激活 |
| 1 | ACTIVE | 已激活 |
| 2 | BANNED | 已禁用 |

### 用户角色 (UserRole)

| 角色码 | 枚举常量 | 描述 |
|--------|----------|------|
| 0 | USER | 普通用户 |
| 1 | ADMIN | 管理员 |

### 商品状态 (ProductStatus)

| 状态码 | 枚举常量 | 描述 |
|--------|----------|------|
| 1 | ON_SALE | 上架 |
| 0 | OFF_SALE | 下架 |
| -1 | DELETED | 已删除 |

---

## 🔐 安全特性

### 1. JWT认证
- 所有需要身份验证的接口都需要携带JWT Token
- Token有效期：24小时
- Token包含用户ID和用户名信息

### 2. 请求限流
- 登录接口：60秒内最多5次请求（基于IP）
- 搜索接口：60秒内最多30次请求（基于IP）
- 下单接口：60秒内最多10次请求（基于用户）
- 使用Redis + Lua脚本实现令牌桶算法

### 3. 并发控制
- 使用Redis分布式锁防止商品超卖
- 订单创建时进行库存扣减，确保库存一致性

---

## 🚀 系统特性

### 1. 性能优化
- Redis缓存：商品信息、用户信息等高频数据缓存
- 批量查询优化：减少数据库查询次数
- 分页查询：支持大数据量的分页展示

### 2. 数据一致性
- 订单超时自动取消：15分钟未支付自动取消订单
- 库存管理：分布式锁保证库存扣减的原子性
- 事务管理：关键操作使用数据库事务保证数据一致性

### 3. 用户体验
- 搜索历史记录：记录用户搜索历史，提供热门搜索
- 商品收藏：用户可以收藏喜欢的商品
- 商品评价：支持用户对商品进行评价和点赞
- AI智能客服：提供智能对话服务

---

## 📝 注意事项

1. **Token管理**：请妥善保管JWT Token，不要泄露给第三方
2. **限流规则**：注意接口的限流规则，避免频繁请求
3. **订单超时**：订单创建后15分钟内需要完成支付，否则会自动取消
4. **库存管理**：下单时会扣减库存，请确保库存充足
5. **商品状态**：只有上架状态的商品才能被购买

---

## 📞 联系方式

如有问题或建议，请联系开发团队。

---

**文档版本**: 1.0  
**最后更新**: 2024-02-22
