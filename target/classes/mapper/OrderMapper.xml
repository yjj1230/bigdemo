<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.shopdemo.mapper.OrderMapper">
    <resultMap id="BaseResultMap" type="org.example.shopdemo.entity.Order">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="coupon_id" property="couponId"/>
        <result column="original_amount" property="originalAmount"/>
        <result column="coupon_discount" property="couponDiscount"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="pay_amount" property="payAmount"/>
        <result column="status" property="status"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="remark" property="remark"/>
        <result column="pay_time" property="payTime"/>
        <result column="ship_time" property="shipTime"/>
        <result column="finish_time" property="finishTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.shopdemo.entity.Order" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `order` (order_no, user_id, coupon_id, original_amount, coupon_discount, total_amount, pay_amount, status, receiver_name, receiver_phone, receiver_address, remark)
        VALUES (#{orderNo}, #{userId}, #{couponId}, #{originalAmount}, #{couponDiscount}, #{totalAmount}, #{payAmount}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress}, #{remark})
    </insert>

    <select id="findById" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM `order` WHERE id = #{id}
    </select>

    <select id="findByOrderNo" parameterType="String" resultMap="BaseResultMap">
        SELECT * FROM `order` WHERE order_no = #{orderNo}
    </select>

    <select id="findByUserId" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM `order` WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM `order` ORDER BY create_time DESC
    </select>

    <update id="update" parameterType="org.example.shopdemo.entity.Order">
        UPDATE `order`
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="payTime != null">pay_time = #{payTime},</if>
            <if test="shipTime != null">ship_time = #{shipTime},</if>
            <if test="finishTime != null">finish_time = #{finishTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE `order` SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="Long">
        DELETE FROM `order` WHERE id = #{id}
    </delete>

    <select id="findTimeoutOrders" resultMap="BaseResultMap">
        SELECT * FROM `order` 
        WHERE status = 0 
        AND create_time &lt; #{timeoutTime}
        ORDER BY create_time ASC
    </select>

    <update id="executeSql">
        ${sql}
    </update>
</mapper>
