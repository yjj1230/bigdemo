<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.shopdemo.mapper.OrderItemMapper">
    <resultMap id="BaseResultMap" type="org.example.shopdemo.entity.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="price" property="price"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" parameterType="org.example.shopdemo.entity.OrderItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_item (order_id, product_id, product_name, product_image, price, quantity, total_price)
        VALUES (#{orderId}, #{productId}, #{productName}, #{productImage}, #{price}, #{quantity}, #{totalPrice})
    </insert>

    <select id="findByOrderId" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM order_item WHERE order_id = #{orderId}
    </select>

    <select id="findByProductId" parameterType="Long" resultMap="BaseResultMap">
        SELECT * FROM order_item WHERE product_id = #{productId}
    </select>

    <delete id="deleteByOrderId" parameterType="Long">
        DELETE FROM order_item WHERE order_id = #{orderId}
    </delete>

    <select id="hasUserPurchasedProduct" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM order_item oi
        INNER JOIN `order` o ON oi.order_id = o.id
        WHERE o.user_id = #{userId}
          AND oi.product_id = #{productId}
          AND o.status = 3
    </select>
</mapper>
